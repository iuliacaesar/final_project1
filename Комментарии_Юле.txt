button.py

__init__()

0. Классы обычно называют с большой буквы, а с маленькой функции
1. ЛУчше указывать дефолтные значения параметров - x, y..., чтобы оно хотя бы выводилось

draw():

2. Нужно предавать window, иначе его просто нет, он даже подчёркивал это
3. Зачем дополнительный аргкмент button_pressed если есть self.pressed?
4. Перепутанны действия в if pressed, потому что если pressed, тогда RED
5. Дабавила параметр size_text в __init__, чтобы можно было настроить размер текста на кнопке

get_pressed():

6. Аналогично, нужно передавать event

get_pressed():

7. Ты и так поменяла значение self.pressed, зачем тебе ещё что-то возвращать, это только усложняет код
